<?php

/**
 * This configuration file is often put in .gitignore, and not put in
 * git. It changes the current system to a non-production system with
 * debugging, so that the checked-in config is production-ready.
 */

return [
    'debug' => true,
    'production' => false,
];
